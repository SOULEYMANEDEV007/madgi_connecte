name: madgi_connect
description: Une application de connexion intelligente

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1  # Pour la gestion d'état
  mobile_scanner: ^3.5.0
  image_picker: ^1.1.1
  qr_flutter: ^4.1.0  # Version stable actuelle
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
